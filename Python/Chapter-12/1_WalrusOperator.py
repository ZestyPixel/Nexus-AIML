list = [1,2,3,4,5]

if ((n:= len(list))<10):
    print(f"Length of the list is {n} which is less than 10")
# This is the walrus operator which assigns the value to a variable and also checks the condition in the same link.